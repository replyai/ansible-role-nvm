---

nvm_version: "master"
nvm_node_version: "\"lts/*\""
nvm_user: vagrant
nvm_working_path: "/home/{{ nvm_user }}"
nvm_dest: "{{ nvm_working_path }}/.nvm"

nvm_npm_pkgs: []
nvm_apt_become: yes
required_packages:
  - git
  - curl
  - libssl-dev
